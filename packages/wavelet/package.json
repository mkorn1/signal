{
  "name": "@signal-app/wavelet",
  "private": true,
  "version": "0.0.1",
  "main": "dist/index.js",
  "type": "module",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && npm run copy-processor",
    "copy-processor": "cp src/processor/processor.js dist/processor.js",
    "dev": "tsc -w",
    "test": "vitest run",
    "lint": "prettier --check src",
    "format": "prettier --write src",
    "diagnose": "npx tsx src/diagnostics/run-diagnostic.ts"
  },
  "author": "ryohey",
  "license": "MIT",
  "dependencies": {
    "@signal-app/sf2parser": "*",
    "midifile-ts": "^1.5.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^4.0.10"
  }
}
